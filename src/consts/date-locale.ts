/**
 * Maps language codes to their corresponding locale formats for date formatting.
 * Used to ensure dates are displayed in the appropriate format based on user language preferences.
 *
 * @constant {Record<string, string>}
 */
export const DATE_LOCALE = {
  pt: 'pt-BR',
  en: 'en-US'
}
